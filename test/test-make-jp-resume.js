#!/usr/bin/env node
"use strict";

let assert = require('assert');
let resume = require('../');

describe("test render html", () => {
    it('hoge', () => {
        assert.equal(1, 1);
    });
    
    
});
